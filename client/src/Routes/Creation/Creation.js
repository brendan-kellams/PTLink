import React, {Component} from 'react';
import {withRouter} from 'react-router-dom';

import {Header, SignUpInput, BasicBtn} from '../../components';

class Creation extends Component {

    state = {
        
    }
}